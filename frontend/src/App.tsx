import './App.css'
import AppRoutes from './routes/AppRoutes'

/**
 * Root application component that renders the application's routing structure.
 *
 * @returns The JSX element containing the application's route hierarchy.
 */
function App() {

  return (
    <AppRoutes />
  )
}

export default App
